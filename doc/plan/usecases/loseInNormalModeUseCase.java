/***
 * Located in Controller.
 *
 * Enemy crosses left border of the screen (player loses)
 */
/*public class loseInNormalModeUseCase {
  private Game modelAPI;
  private GameView viewAPI;

  public loseInNormalModeUseCase() {
    modelAPI = new GameConcrete();
    viewAPI = new GameViewConcrete();
  }

  private void checkAndHandleCrossingLosingCondition() {
    //check in model's API whether enemy has moved past the leftmost side, this is the losing condition
    if(modelAPI.enemyHasMovedPastLeftBorder()) {
      //displays in the view the fact that the game was just lost
      viewAPI.displayGameEnding(false);
    }
  }
}*/